# Example main Caddyfile for FrankenPHP Panel–managed sites
# Copy to /etc/caddy/Caddyfile and reload Caddy.
#
# This imports site blocks generated by the panel (Add Site) into /etc/caddy/sites/
# Each site gets: root /var/www/<domain>, php_server, file_server.

{
	# Optional: global options
}

# Import all panel-created site configs (one file per domain)
import /etc/caddy/sites/*

# Optional: panel UI (e.g. panel.example.com)
# panel.yourdomain.com {
#     reverse_proxy 127.0.0.1:2090
# }
